{{!< default}}
<div class="gh-page flex">
    <div class="gh-container">
        <div class="bg-heavy my-24">
            hello world
            <div>

            </div>
        </div>

        <div class="gh-postfeed post-feed">
        {{#foreach posts}}

            {{> "loop" page=../pagination.page }} {{!-- partials/card.hbs --}}

        {{/foreach}}
        </div>

        {{!-- If JS isn't enabled, fallback to standard pagination --}}
        <noscript>
            <div class="gh-pagination">
                {{pagination}}
            </div>
        </noscript>
    </div>
</div>