{{!< default}}

<div class="gh-page">
  {{#get "tags" include="count.posts" filter="slug:'{{tag.slug}}'"}}
    {{#foreach tags}}
      {{> "section/hero"}}
    {{/foreach}}
  {{/get}}
  

  {{> "section/title" title="글 목록" subtitle="article list"}}
  {{> "section/article-inf"}}
</div>