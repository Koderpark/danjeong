{{#get "tags" limit="all" include="count.posts" order="count.posts desc"}}
  {{#foreach tags}}
    <a href="{{url}}">
      <h2>{{name}} <small>({{count.posts}})</small></h2>
    </a>
  {{/foreach}}
{{/get}}