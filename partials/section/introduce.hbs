<div class="gh-container flex gap-4 grid-rows-2">
  <div class="mt-16 bg-heavy rounded-xl w-full h-full flex flex-row">
    <div class="p-4">
      <img src="{{@site.logo}}" alt="{{@site.title}}" class="rounded-xl logo" />
    </div>
    <div class="grow py-4">
      <p class="text-2xl font-bold mb-2">{{@site.title}}</p>
      <p class="text-xl">{{@site.description}}</p>
    </div>
  </div>
  <div class="mt-16 bg-heavy rounded-xl w-full h-full flex flex-row">
    <div class="p-4">
      <img src="{{@site.logo}}" alt="{{@site.title}}" class="rounded-xl logo" />
    </div>
    <div class="grow py-4">
      <p class="text-2xl font-bold mb-2">{{@site.title}}</p>
      <p class="text-xl">{{@site.description}}</p>
    </div>
  </div>
</div>